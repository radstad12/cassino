import {motion} from 'framer-motion';import type {ActionType} from '../game/types';
export default function ActionPanel({actions,onAction,raise,onRaise}:{actions:ActionType[];onAction:(a:ActionType)=>void;raise:number;onRaise:(n:number)=>void}){return <div className="action-panel"><div className="raise-control"><span>RAISE TO</span><strong>${raise.toLocaleString()}</strong><input type="range" min={raise} max={Math.max(raise,2000)} step={50} value={raise} onChange={e=>onRaise(+e.target.value)}/></div><div className="actions">{actions.map(a=><motion.button whileTap={{scale:.96}} key={a} className={`action ${a}`} onClick={()=>onAction(a)}>{a==='raise'?'RAISE':a.toUpperCase()}</motion.button>)}</div></div>}
