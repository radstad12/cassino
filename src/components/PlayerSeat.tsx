import type {Player} from '../game/types';import ChipStack from './ChipStack';
export default function PlayerSeat({p,active,winner,showCards}:{p:Player;active:boolean;winner:boolean;showCards:boolean}){return <div className={`seat seat-${p.id} ${active?'active':''} ${winner?'winner':''} ${p.folded?'folded':''}`}><div className="avatar">{p.avatar}</div><div className="seat-info"><b>{p.name}</b><span>${p.chips.toLocaleString()}</span></div>{p.currentBet>0&&<div className="bet"><ChipStack amount={p.currentBet}/><span>${p.currentBet.toLocaleString()}</span></div>}{showCards&&<div className="mini-cards">{p.hand.map(c=><span key={c.id} className={(c.suit==='♥'||c.suit==='♦')?'red':''}>{c.rank}{c.suit}</span>)}</div>}</div>}
